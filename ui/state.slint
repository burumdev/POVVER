export struct Date {
   minute: int,
   hour: int,
   day: int,
   month: int,
   year: int,
}

export struct MonthData {
    day_start: int,
    day_end: int,
    name: string,
    sunshine_factor: float,
    windspeed_factor: float,
    cloud_forming_factor: float,
}

export struct TimerData {
    date: Date,
}

export enum SunStage {
	Set,
	Weak,
	Normal,
	Bright
}

struct SunData {
	position: int,
	brightness: float,
	brightness_reduction: float,
	stage: SunStage,
}


export enum CloudSize {
	Small,
	Medium,
	Big
}

export struct Cloud {
	size: CloudSize,
	position: int,
	image_index: int,
	image_rotated: bool,
}

export enum WindDirection {
	Ltr,
	Rtl
}

export enum WindSpeedLevel {
    Faint,
    Mild,
    Strong,
    Typhoon
}

export struct EnvData {
	the_sun: SunData,
	wind_speed: int,
	wind_direction: WindDirection,
	wind_speed_level: WindSpeedLevel,
	clouds: [Cloud],
}

enum UpDown {
	Up,
	Down
}

export struct MacroEconData {
	inflation_rate: float,
	inflation_direction: UpDown,
	fuel_price: float
}

export struct UIState {
    is_paused: bool,
    speed_index: int,
}

export enum UIMessageSource {
	Hub,
	PP,
	Factory
}

export enum UILogLevel {
	Info,
	Warning,
	Critical
}

export struct UILogMessage {
	source: UIMessageSource,
	message: string,
	level: UILogLevel,
	factory_id: int,
}