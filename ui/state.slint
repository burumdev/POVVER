struct UIDate {
   minute: int,
   hour: int,
   day: int,
   month: int,
   year: int,
}

struct TimerData {
    date: UIDate,
    month_name: string,
}

export struct UIState {
    timer: TimerData,
    is_paused: bool,
}
