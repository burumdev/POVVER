import { UIState, CloudData } from "./state.slint";

export global GlobCallbacks {
    pure callback toggle_pause;
    pure callback speed_change(int);
}

export global Globs {
    in property <UIState> state;
    in property <[CloudData]> clouds;
}
