import { Palette } from "../../theme.slint";
import { DateTimeControls } from "date-time.slint";
import { PanelTabs } from "./tabs.slint";

export component ControlPanel inherits Rectangle {
    width: 20%;
    height: 100%;

    VerticalLayout {
		DateTimeControls {}

		PanelTabs {}
        Rectangle {
            background: Palette.bg_darkgray;
        }
    }
}
